# ============================================================
# FRONTEND .env.example — copia a frontend/.env
# ============================================================
# IMPORTANTE: Todo lo que empiece con VITE_ se expone en el bundle
# público del navegador. NUNCA pongas secretos aquí (API keys, tokens,
# passwords). Sólo config pública.

VITE_API_URL=http://localhost:8000
